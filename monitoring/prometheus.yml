global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/rules/alert.rules.yml

scrape_configs:
  - job_name: 'my-duit'
    static_configs:
      - targets: ['host.docker.internal:3000']

alerting:
  alertmanagers:
    - static_configs:
        - targets: ['alertmanager:9093']
